#include <stdio.h>
#include <cs50.h>


int main(void)
{
    int arr[11] = {3, 7, 4, 2, 0, 5, 2, 4, 8, 0, 4};
    int iter = 0;
    int, index, index_2;
    while (iter < 11)
    {
        for (int i = 0; i < 9; i++)
        {
            if (arr[i] > arr[i + 1])
            {
                index = i, index_2 = i + 2;

                arr
            }
        }
        iter++;
    }

}